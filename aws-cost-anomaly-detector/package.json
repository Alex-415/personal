{
  "name": "aws-cost-anomaly-detector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:frontend": "wrangler pages deploy frontend --project-name=aws-cost-anomaly-detector",
    "db:init": "wrangler d1 execute cost-anomaly-db --local --file=database/schema.sql",
    "db:init:prod": "wrangler d1 execute cost-anomaly-db --file=database/schema.sql",
    "db:studio": "wrangler d1 studio cost-anomaly-db"
  },
  "dependencies": {
    "aws4fetch": "^1.0.18",
    "hono": "^4.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
